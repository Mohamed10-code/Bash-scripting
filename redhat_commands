# ps command 
ps -u root: will show you all the process rin by the root 
ps -ef: to see every running process on the system
nice -n sleep 100: you will give sleep high process value 
recover root password: restart your computer then edit the grup change the password then reboot 
--sudo dnf install vim -y &>/dev/null && echo successfull # if the vim is successfully downloaded it will print successfull on the screen dev/null is black hole for linux mean whatever you direct there will be lost 
--nmcli is network manager command line interfer it shows which network is connected to the your machine  
-- lscup |grep -i '^cpu(s)' will show you have many cpu you have
--(man strftime) --command it view option of format date and time.. try it very nice and helpfull
#--cockpit  is web based console which very usefull the command is localhost:192.168.122.179
--time is command that count how many minutes took the command to execute

#This for cockpit web based console command it very usefull 
https://server-ip:9090

NIC = Network Interface Card (PC or Laptop) 
NIC bonding is also known as network bonding it can be defined as the aggregation or 
combination of multiple NIC into a single bond interface 

#USING NMCLI TO CONFIGURE STATIC IP
nmcli connection modify enpo0s3 ipv4.addresses 10.255.255.255/24
 
nmcli connection modify enpo0s3 ipv4.gateway 10.255.1.1/24
nmcli connection modify enp0s3 ipv4.method manual 
nmcli connection modify enp0s3 ipv4.dns 8.8.8.8
nmcli connection down enp0s3 $$ nmcli connection up enp0s3
ip address show enp0s3
#This command to assign your Machine static IP 
#
#SSH file to edit
sudo vim /ect/ssh/sshd_conf #This where you can edit all of ssh login and security hardening 

#How does ssh-key works 
--if you want to login to your sever withiout password 
you can generate ssh-key on your machine 
[command for generating the ssh-key is ssh-keygen ]
after you generate the key the key will be store in [~/.sshd]
then you can copy the ssh-key to the server 
by ssh-copy-id moha@192.168.122.1




#FTP - FILE TRANSFER PROTOCOL CONFIGRE ON THE SERVER 
--installing and configure FTP on the remove server 

-become root 
-rpm -qa |grep ftp 
yum install vsftpd
vim /etc/vsftpd/vsftpd.conf
#enable this lines 
anonymous_enable=NO
#uncommnet this lines 
ascii_upload_enable=YES
ascii_download_enable=YES
# Add at the end of the line 
use_localtimes=yes
# ON THE CLIENT SIDE 
Become root 
install ftp 
su - moha 
touch kruger (kruger is the name of the file..)

#transfer file from client to server using FTP 
ftp --ip--for-server
then enter the username and the password when you see successfully logined in
bi 
hash 
put kruger (kruger is the name of the file you want to transfer)
bye --- for exit 
#SCP >>secure copy protocol 
scp filename moha@192.122.122.122:/home/moha
it will ask you the password of the moha(note moha is the name of  the server means you are sending file to moha)
#Tar the file 
tar cvf backup.tar .  (. means the current file or home)


#rsync command 
rsync -azvh /tmp/backups/ mohamed@192.16.1.1:/tmp/backups/ ----
#--------------------------------------------------------- 
 (rsync -azvh is the command)(/tmp/backup is the file i was sending to the 
server) (mohamed@192.112..12. is the server)and (/tmp/backups is the destination )

# when you want to download from the server 
-- rsync -avzh moha@192.168.12.9:/home/moha/serverfile /home/mohamed 
(moha@192 is the name of the server)
(/home/moha/serverfile---is the path of the file that you wanna download)
and (/home/mohamed is where you dowload it too)                            

#This command is specific to sending redhat_commands from the server to my local machine
rsync -azvh script/redhat_commands mohamed@192.168.122.1:/home/mohamed/redhat_commands
#-------------------------------------------------------------------------------------

# To find specific file with owner and group you can you this command 
#
find / -type f -user bandit7 -group bandit6 -size 33c >2/dev/null
sort data.txt | uniq -c | grep " 1 " this command will help you to extarct line with no repeat 

# if you wanna create firewall-cmd for 3rd party you can go to this file and edit 
# /usr/lib/firewalld/services 
the you create xml file then you can vim into and create configuration 
the easiest way is by copying one file and modify accord to your needs 

#firewalld is the enginee and firewall-cmd is the cli 
the basic command is 
filewall-cmd --add-services=http
filewall-cmd --list-all
firewall-cmd --get-services
firewall-cmd --add-port=1110/tcp
#to reject incoming traffic from ip address use this command 
#

firewall-cmd -add-rich-rule-='rule family="ipv4" source address="192.168.122.177" reject'

# sudo firewall-cmd  --permanent --add-icmp-block-inversion   (this command will make your server not pinged)
#
# to block specific website you can use this command 
sudo firewall-cmd  --permanent  --direct --add-rule ipv4 filter OUTPUT 0 -d 57.144.138.1 -j DROP    (57.144.138.1 is ip for facebook)
then do 
sudo firewall-cmd --reload 
host -t a www.facebook.com  (this command will get the ip of the website ) 


# A dynamic system tuning daemon for Linux
tuned-adm active                    # Show current active profile
tuned-adm list                      # List all available profiles
tuned-adm recommend                 # Get profile recommendation
tuned-adm profile_info [profile]    # Show details of a profile

tuned will tell you which profile you have use for aws you should aws-profile
for monitor and daily usaged use desktop 

#this command will help to view who is running with higher priority
#the actual command is nice and nice will give process higher priority 
nice -n -15 top 
ps axo pid,comm,nice,cls --sort=-nice

#configuration file for kitty terminal
sudo vim ~/.config/kitty/kitty.conf

#command for podman 
podman run -dt -p 8082:80/tcp docker.io/library/httpd (this command will create contain for httpd)
sudo podman pull docker.io/arm32v7/httpd (command to download podman image)
podman ps (list podman)
podman stop ID
podman images (shows you the downloaded images)
podman logs -l (logs of the podman)
podman search httpd   (search httpd premaid container)
podman image exists


#Docker 
#
docker run -it -p 8080:80 ubuntu bash
apt update && apt install -y nginx
service nginx start
# From host: http://localhost:8080
this command will help you run docker and create ubuntu contain and test your web server nginx 
docker container run -itd --name web-server-nginx -p 8080:80 nginx:latest 
this command will help you create nginx in container 
dockerfile (build)
docker image (ship)
container (run)
 
To build docker image for yourself 
you need cd in /ect/docker and then touch dockerfile 
then vim into it and create this code 

ARG CODE_VERSION=16.04

FROM ubuntu:${CODE_VERSION}

RUN apt-get update -y

CMD ["bash"]

after that you can do sudo docker -t build img_from .
then do will build custom image for you 





#Linux Boot Process
  
#BIOS 
basic input/output system executes MBR
#MBR 
master boot record executes GRUB 
#GRUB
grand unified bootloader executes 
#KERNEL
kernel executes/sbin/init
#INIT 
executes runlevel program 
#RUNLEVEL
runlevel program are executes from /etc/rc.d/rc*.d/


#CUSTOMIZE MESSAGE OF THE DAY 
#
once again, message of the is the first message users will see when 
they login to the linux machine 

STEPS 
create a new file in  /etc/profile.d/motd.sh 
add desired commands in motd.sh file 
Modify the /etc/ssh/sshd_config file to edit 
#PrintMotd yes to PrintModt no
Restart sshd services by
sudo systemctl reload sshd

#cheat command it like man page or tldr 
#but more advance 
sudo pip3 install cheat 
this command ill install cheat via 
python3 

And the command is cheat commandname


#Swap memory is virtual memory for your machine
#
sudo dd if=/dev/zero of=/newswap bs=1M count=1024
this command will help you create swap memory 

#This command will help you resize your Qemu-VM
sudo qemu-img resize /var/lib/libvirt/images/centos-stream9.1774449128 +10G   
then you can you this command -lsblk-




























